git init
git add -A
git status
git commit -m "Initial"


git config --global user.name "Pavel Mamaev"
git config --global user.email p_mamaev@mail.ru
git checkout -b function1

git commit  -am "11"

type readme.txt

Function2